# 单元 09-1｜预训练、继续预训练与 SFT

> 所属章节：[第 9 章｜后训练与对齐](../09-post-training-alignment.md) · 预计用时：约 **15 分钟**

::: info 本单元目标
围绕 **预训练、继续预训练与 SFT** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

预训练语言模型学习的核心目标是预测下一个 Token。给它一段文本，它更像是在续写互联网上可能出现的后文，而不是天然知道“用户提问后应该给出有帮助、安全、格式正确的回答”。

例如输入：

```text
用户：请用三点解释什么是 KV Cache。
```

Base Model 可能继续生成另一段论坛对话、网页内容，甚至补写“用户”的下一句话。要让它稳定扮演助手，还需要使用示范数据和偏好数据塑造行为。

![从 Base Model 到 Assistant 的典型训练流水线](/images/llm/model-training-stages.svg)

可以先记住三个层次：

- **预训练**主要形成通用语言、知识和模式能力；
- **SFT**主要教模型按照指令和对话格式作答；
- **偏好对齐**主要让模型在多个可行回答之间更偏向人们希望的行为。

这些边界不是绝对的：高质量 SFT 也会带来新知识和能力，继续预训练也会改变行为。但三者的数据规模、目标函数和工程作用不同。

## 2. 预训练：从海量 Token 中学习分布

Decoder-only 模型对序列 $x_1,x_2,\ldots,x_T$ 最大化：

$$
P(x_1,\ldots,x_T)=\prod_{t=1}^{T}P(x_t\mid x_{<t})
$$

等价地，训练时最小化所有预测位置的交叉熵：

$$
L_{pretrain}=-\sum_{t=1}^{T}\log P_\theta(x_t\mid x_{<t})
$$

训练数据可以包含网页、书籍、代码、论文等。经过大规模预训练后，模型学习到：

- 词法、语法和文本结构；
- 常见事实与领域模式；
- 代码和自然语言之间的对应关系；
- 从上下文继续推断的能力。

预训练计算量最大、数据量最多，通常决定模型能力的上限，但它没有直接规定一个统一的助手行为。

## 3. 继续预训练与 SFT 不要混淆

### 3.1 Continued Pretraining

继续预训练仍然使用下一个 Token 预测目标，只是将数据换成特定领域，例如金融报告、医疗文献或企业代码。它让模型进一步适应该领域的词汇和分布。

```text
领域原始文本 / 代码 → 按原始语言模型目标训练
```

### 3.2 Supervised Fine-Tuning

SFT 数据通常是明确的输入输出示范：

```text
System：你是一个严谨的技术助手。
User：为什么训练前需要清空梯度？
Assistant：因为 PyTorch 默认会把新梯度累加到 parameter.grad……
```

模型仍然做 Token 级交叉熵训练，但监督目标重点落在期望回答上：

$$
L_{SFT}=-\sum_{t\in Assistant}\log P_\theta(y_t\mid x,y_{<t})
$$

$x$ 是指令和上下文，$y$ 是目标回答。常见实现会将 System/User 部分对应的 Label 设为忽略值，只计算 Assistant Token 的 Loss；也存在对完整序列计算 Loss 的训练方案，必须看具体数据模板和 Trainer 实现。

![继续预训练与 SFT 使用不同的数据形态和监督位置](/images/llm/pretrain-vs-sft.svg)

SFT 学到的不仅是答案内容，还包括：

- 角色和对话模板；
- 指令遵循方式；
- 输出格式和风格；
- 工具调用的结构；
- 拒答与安全示范。

---

[继续单元 09-2 →](./02-full-finetune-lora.md)
