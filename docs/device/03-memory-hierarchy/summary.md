# 第 3 章总结｜存储层次与数据搬运

> 返回：[第 3 章首页](../03-memory-hierarchy.md)

## 把本章串成一条主线

请先合上各单元正文，沿"物理矛盾 → 金字塔 → 每层身份证 → 搬运账本 → 局部性对策"复述整章；遇到断点时，再回到对应单元查阅。

```text
快与大不可兼得（物理矛盾）
        ↓
存储金字塔：寄存器 → 片上 SRAM → HBM → DDR/盘（03-1）
        ↓
片上 SRAM 的两种哲学：Cache 自动 vs Buffer 显式（03-2）
GPU：寄存器 + SharedMem/L1 + L2 + HBM
NPU：寄存器 + UB/L1/L0 + DMA/MTE 显式搬运 + HBM
        ↓
搬运的代价：能耗数百倍 · 带宽定时间下界（03-3）
        ↓
对策：少搬 · 重用 · 搬巧（03-4）
        ↓
第 4 章：把"重用"工程化为 Tiling 与流水线
```

## 9. 动手练习

### 练习 1：给两个算子记账

FP16 数据，两个 $4096 \times 4096$ 的输入矩阵：

1. 逐元素加法 `y = x + w`：计算 FLOPs、实际搬运字节、复用次数 R；
2. 方阵乘法 `y = x @ w`：计算 FLOPs、最少搬运字节（假设只用一遍输入）、复用次数 R；
3. 设备有效带宽 2 TB/s、峰值算力 400 TFLOPs，分别计算两个算子的访存与计算时间下界，判断各自的瓶颈。

### 练习 2：Tile 大小的权衡

片上 Buffer 可用 256 KB。分块 GEMM 中每个输入 Tile 为 $[T, T]$ 的 FP16 块（每块 $2T^2$ 字节）。回答：T 最大能取多少（两个输入块同时驻留）？T 取太大和太小各损失什么？如果输出块也要驻留，T 上限变为多少？

### 练习 3：解释一次布局转换的开销

某推理服务在两个算子之间插入了显式 Transpose，输入输出均为 $[8, 4096, 4096]$ 的 FP16 张量。估算这次转换读写 HBM 的总字节；用本章概念说明为什么它可能比看起来"只是挪一下数据"贵得多，并给出两条消除它的思路。

## 10. 自测题

1. 为什么存储不能一层做到"又大又快又便宜"？
2. 容量、延迟、带宽三个指标分别决定什么？
3. Cache 和 Buffer 的核心区别是什么？各举一个代表。
4. HBM 与 DDR 的定位差异是什么？
5. 寄存器溢出（Spill）为什么会显著降低性能？
6. 从能耗视角解释"计算免费、搬运昂贵"。
7. 复用次数 R 怎么定义？逐元素算子和大 GEMM 的 R 量级差多少？
8. 时间局部性和空间局部性分别是什么？各对应什么优化？
9. Tile 尺寸选择的本质权衡是什么？
10. 布局转换为什么有真实成本？什么时候值得主动做转换？

::: details 自测答案

1. 工艺上快存储（SRAM）面积大、成本高，无法做大容量；大容量存储（DRAM/HBM）物理上离计算单元远、器件慢。三者相互矛盾，只能分层组合。
2. 容量决定什么数据必须驻留在片外；延迟决定单次等待时长（GPU 用 Warp 切换、NPU 用流水隐藏）；带宽决定持续数据供给的上限。
3. Cache 由硬件按访问历史自动管理（CPU 各级 Cache、GPU L1/L2）；Buffer 由软件显式指定装什么（GPU Shared Memory、NPU UB/L1/L0）。前者透明但不可控，后者可预期但编程负担重。
4. DDR 是 CPU 侧系统内存，容量大带宽相对低；HBM 通过 3D 堆叠贴近加速芯片，容量小一个量级但带宽高数倍，是放权重、激活和 KV Cache 的地方。
5. 溢出把本应留在寄存器的数据赶回片下存储，每次使用都要重新经历长延迟和带宽消耗。
6. 一次 HBM 读取的能耗是一次寄存器内乘加的数百倍以上；若每个字节只算一次就搬运，能量几乎全部花在搬运上，计算开销可忽略。
7. R = 计算次数 / 搬运字节次数。逐元素算子 R 约 1/3；GEMM 中每个输入元素被复用 N 或 M 次，大矩阵下 R 可达数百上千——相差三个数量级，决定了前者天生 Memory-bound、后者可优化到 Compute-bound。
8. 时间局部性：刚用的数据很快再用，留在最快层；空间局部性：相邻地址即将被用，成块搬运。分别对应数据驻留/融合与连续访问/分块。
9. 本质是在片上容量约束下最大化复用次数：块越大复用越高、搬运越少，但受 Buffer 容量与寄存器压力限制，还要兼顾并行度和尾块。
10. 转换需要真实读写 HBM（一趟读 + 一趟写），且可能破坏访问局部性。当转换后的布局能让后续多个算子的访问/分块效率显著提升、或能与计算融合摊薄成本时，才值得做。

:::

## 11. 本课小结

- 存储金字塔是物理约束的必然：越近越快越小越贵；
- 片上 SRAM 分两种哲学：Cache 硬件自动、Buffer 软件显式，GPU 两者兼备，NPU 以 Buffer 为主；
- 搬运比计算贵：能耗差数百倍，带宽决定时间下界；
- 复用次数 R 统一账本：R 低天生 Memory-bound（融合出路），R 高可优化到 Compute-bound（Tiling 出路）；
- 局部性是软件的抓手：时间局部性靠驻留与融合，空间局部性靠连续访问与分块；
- Layout 是双刃剑：匹配访问模式则快，不匹配则引入昂贵的隐式转换。

## 参考资料

- [NVIDIA CUDA C++ Programming Guide：Memory Hierarchy](https://docs.nvidia.com/cuda/cuda-c-programming-guide/)
- [CANN Learning Hub：NPU 实践](https://gitcode.com/cann/cann-learning-hub/blob/master/quick_start/cann_basics/04_npu_practice.ipynb)
- [昇腾官方文档：AI Core 硬件架构](https://www.hiascend.com/document/detail/en/canncommercial/800/opdevg/Ascendcopdevg/atlas_ascendc_10_0008.html)
- [2026 主流 AI 加速卡全景](../accelerator-cards-2026.md)

下一课将进入 **Kernel、Tiling 与流水线**：一个大算子怎样被切成 Tile，搬运和计算怎样形成流水——把本章的复用账本变成可执行的代码结构。

---

<!-- chapter-navigation -->
[返回专栏目录 →](../index.md)
