# 第 1 章｜AI Infra 全景与性能分析方法

> 本课目标：理解 AI Infra 各层分别负责什么；能够把一次模型执行拆成框架、编译、Runtime、Kernel 和硬件阶段；建立计算、访存、通信三类瓶颈的基本判断方法；学会用“现象—指标—证据—结论”分析性能问题。

## 本章导学

::: tip 本课只记住 3 件事
1. 模型代码要经过框架、编译、Runtime 和 Kernel，最终才在硬件执行。
2. 性能问题先区分计算、访存、通信与等待，并寻找真正的关键路径。
3. 好的性能结论来自可复现基线和对照实验，不来自“我感觉是这里慢”。
:::

**学习节奏：** 本章拆为 **5 个学习单元，每个约 15 分钟**。先判断数据与任务在哪里，再进入性能公式。

**跟不动？** 先读 [⚡ 速通版（约 5 分钟）](#⚡-速通-约-5-分钟)：一个餐厅类比讲完整章，再回来按单元深入。

**先看这几张核心图：** Model to Device、Timeline Overlap、Performance Funnel。

- [ ] 我能画出模型到设备的软件栈
- [ ] 我能区分三类核心瓶颈
- [ ] 我能写出一个可验证的性能假设

## 本章单元

- **01-1（约 15 分钟）**：[AI Infra 分层与执行对象](#_01-1-ai-infra-分层与执行对象)
- **01-2（约 15 分钟）**：[性能指标与时间线](#_01-2-性能指标与时间线)
- **01-3（约 15 分钟）**：[计算、访存与通信瓶颈](#_01-3-计算、访存与通信瓶颈)
- **01-4（约 15 分钟）**：[数量级、算术强度与峰值](#_01-4-数量级、算术强度与峰值)
- **01-5（约 15 分钟）**：[性能分析流程与常见误区](#_01-5-性能分析流程与常见误区)

- **本章总结**：[串联知识、练习与检查](#本章总结)

## ⚡ 速通（约 5 分钟）

> 适用：时间紧，或完整版跟不动时。这页用一家餐厅讲完整章，读完抓住 80% 的主干。任何一节想深入，拉到文末点对应单元。

### 1. 你写了一行代码，然后呢？

你的模型代码像一份**菜谱**：它写的是"我要一道宫保鸡丁"，不是"怎么炒"。

菜谱自己不会变成菜。它要经过一条链：

```text
菜谱（你的 PyTorch 代码）
  → 前台接单（框架：检查点什么菜、料够不够）
  → 后厨翻译（编译器：把菜谱变成具体工序，用哪口锅几分钟）
  → 后厨调度（Runtime：安排灶台，记住先来后到）
  → 灶台开火（硬件：厨师真的开始炒）
```

两个容易混淆的词，用餐厅区分：**Host** 是前台经理（CPU），**Device** 是后厨（NPU/GPU 加速卡）。你的代码在前台跑，真正的炒菜在后厨。

::: details 小测 1：为什么 NPU 看不懂你的 Python 代码？
因为硬件只认自己的"炒菜动作"（指令），不认菜谱。中间必须有人一层层翻译：框架 → 编译器 → Runtime → 硬件。这整条翻译链，就是 AI Infra 要研究的东西。
:::

### 2. 餐厅只会以三种方式变慢

假设生意慢了，原因逃不出三种，对应三种"慢"：

**① 厨师忙不过来（计算瓶颈，Compute-bound）。** 订单排到门口，厨师从开火到打烊没停过。这时再加快进货也没用——瓶颈在人。

**② 食材运不进来（访存瓶颈，Memory-bound）。** 厨师站着干等食材：仓库（显存）到操作台（片上小台面）的路太窄。厨师再快也白搭——瓶颈在路。

**③ 分店之间传菜（通信瓶颈，Communication-bound）。** 多店协作一桌宴席，一辆送餐车堵在路上，所有分店等它一个人。多卡训练就是这种宴席——瓶颈在路上的车。

::: warning 关键
三种慢的药方完全不同：缺人加人、路窄修路、车堵疏车。**先分清是哪种，再动手**。而且它们常常轮流出现：这个时段缺人，下个时段堵车。
:::

::: details 小测 2：厨师全程有空，订单却没变快，最可能是哪种？
② 或 ③——计算单元闲着，说明菜没送到手上：要么食材运不进来（访存），要么等别家分店的数据（通信）。
:::

### 3. 怎么判断哪里慢：先计时，再下结论

好的餐厅经理不问厨师"你觉得哪里慢"，而是**看监控录像**——对应到我们的世界就是 Profiling（性能分析工具）：把每个环节花了多少毫秒摊开看。

看监控时问三个问题：

1. **食材现在在哪？**（数据在仓库还是操作台，在哪家分店）
2. **有多少食材？**（数据的字节数）
3. **谁在等谁？**（哪个工位停着，在等什么）

还有一个聪明的**记账法**：每份食材能出几道菜。术语叫**算术强度**：计算量 ÷ 搬运量。

- 解冻一整头牛只做一盘牛肉——搬运太亏，天生容易②慢；
- 一筐土豆做一百盘薯条——搬得值，值得雇更好的厨师（①慢才是瓶颈）。

对于"天生搬运亏"的菜，出路不是修路，是**合并菜单**（把几道工序合成一道，食材别来回搬）。

::: details 小测 3：每搬 1 字节只做 0.2 次计算的算子，大概率卡在哪？
② 访存。搬运太亏（算术强度太低），计算单元基本在等数据。优化方向是合并工序/融合算子，而不是让"厨师"更快。
:::

### 3 句话带走

1. 一行代码要穿过 **前台→翻译→调度→灶台** 才变成结果，每一层都可能拖慢；
2. 慢只有三种：**厨师**（计算）、**食材**（访存）、**传菜**（通信），先分清再动手；
3. 好的优化像好的经理：**看监控计时、不凭感觉**；每次只改一处，改完复测。

### 黑话小词典

| 术语 | 人话 |
| --- | --- |
| Host / Device | 前台经理（CPU）/ 后厨（NPU、GPU） |
| 算子 / Kernel | 菜谱里的一道工序 / 这道工序在具体灶台上的炒法 |
| Runtime | 后厨调度：管灶台、排顺序、传菜 |
| Stream | 一条"先来后到"的排队通道 |
| 延迟 / 吞吐 | 一单出餐多快 / 一晚能出多少单 |
| Compute-bound / Memory-bound | 厨师忙死 / 食材运不动 |
| 算术强度（AI） | 每份食材出几道菜：计算量 ÷ 搬运量 |

### 想深入？

每节 15 分钟，按需点开，不必按顺序全读：

| 单元 | 讲什么（白话） | 什么时候需要它 |
| --- | --- | --- |
| [01-1 分层与执行对象](#_01-1-ai-infra-分层与执行对象) | "前台到灶台"那条链，每层具体干什么 | 想搞清一段代码到底经过了哪些软件 |
| [01-2 指标与时间线](#_01-2-性能指标与时间线) | 怎么读"监控录像"（时间线） | 想看懂 Profiling 工具的输出 |
| [01-3 三类瓶颈](#_01-3-计算、访存与通信瓶颈) | 三种慢各自的证据长什么样 | 拿到数据后想判断瓶颈类型 |
| [01-4 数量级与算术强度](#_01-4-数量级、算术强度与峰值) | 餐厅账本的精算版 | 想在打开工具前先粗估瓶颈 |
| [01-5 分析流程与误区](#_01-5-性能分析流程与常见误区) | 餐厅经理的标准工作法 | 想避免"感觉是这里慢"式踩坑 |
| [本章总结](#本章总结) | 动手练习 + 10 道自测 | 想检验整章掌握程度 |

## 01-1｜AI Infra 分层与执行对象

::: info 本单元目标
围绕 **AI Infra 分层与执行对象** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

大模型基础关注“模型要计算什么”，AI Infra 关注“这些计算怎样在真实系统中高效完成”。

一个完整的大模型系统不仅有模型，还包括：

```text
模型与算法
  → 训练/推理引擎
  → 深度学习框架
  → 图编译与算子
  → Runtime、驱动与通信库
  → GPU/NPU、内存与互联网络
```

AI Infra 的工作不是单纯让某个 Kernel 更快，而是在正确性、性能、成本、稳定性和可维护性之间做系统性优化。

常见目标包括：

- **放得下**：模型参数、激活、优化器状态和 KV Cache 不超过容量；
- **算得快**：充分利用矩阵、向量等计算单元；
- **搬得快**：减少 HBM、Host 与设备、设备之间的数据搬运；
- **扩得开**：设备数量增加后仍获得有效加速；
- **跑得稳**：长时间任务不会因抖动、超时或单点故障频繁中断；
- **用得省**：用更少设备、更低能耗完成相同工作量。

### 2. 从一行模型代码到设备执行

假设模型代码中有一行矩阵乘法：

```python
y = x @ weight
```

它并不等于“Python 直接控制计算单元做乘法”。中间会经过多层系统：

![一段模型计算如何穿过 AI Infra 软件栈](/images/ai-infra/model-to-device-execution.svg)

#### 2.1 模型与框架层

框架接收 Tensor 和算子调用，检查 Shape、DType、Device 等信息，并构建动态图或静态计算图。

这一层关心：

- 输入输出是什么；
- 是否需要梯度；
- 算子之间有什么依赖；
- 张量位于哪个设备。

#### 2.2 图编译与算子层

编译器或算子分发系统为当前 Shape 和数据类型选择实现，也可能进行算子融合、常量折叠、内存复用和调度优化。

这一层关心：

- 使用哪个 Kernel；
- 是否需要重新编译；
- 数据布局是否匹配；
- 多个算子能否融合；
- 工作空间需要多少内存。

#### 2.3 Runtime 与驱动层

Runtime 管理设备内存、Stream、Event 和任务提交。Host 通常只是把任务放入设备队列，设备随后异步执行。

这一层关心：

- 任务在哪条 Stream 上；
- 任务之间怎样同步；
- 数据何时搬入或搬出；
- Kernel Launch 是否形成空隙；
- 错误怎样返回 Host。

#### 2.4 硬件层

设备从 HBM 读取数据，搬到更靠近计算单元的片上存储，再由矩阵或向量单元执行，最后写回结果。

这一层关心：

- 计算单元是否吃满；
- HBM 带宽是否饱和；
- 片上空间是否足以复用数据；
- 访存是否连续、对齐；
- 不同流水阶段是否互相等待。

#### 2.5 通信层

如果张量被切到多张设备，原本的本地数据依赖会变成通信。框架调用通信库，通信库根据 Rank、拓扑和消息大小选择算法，再由链路真正传输。

对 HCCL 开发来说，需要同时理解：

```text
上层为什么需要这次通信
      ↓
通信前后的张量布局怎样变化
      ↓
HCCL 怎样把语义拆成传输与归约任务
      ↓
任务怎样落到 Stream、链路和设备执行
```

### 3. Host、Device、Kernel、算子分别是什么

这些词经常一起出现，但含义不同。

| 概念 | 基本解释 |
| --- | --- |
| Host | 通常指运行应用程序、框架和调度逻辑的 CPU 侧 |
| Device | 实际执行大规模张量计算的 GPU/NPU 等加速设备 |
| 算子（Operator） | 面向模型语义的计算单元，例如 MatMul、Softmax、AllReduce |
| Kernel | 算子在特定硬件、Shape、DType 和布局下的一种底层实现 |
| Runtime | 管理内存、Stream、Event、任务下发和设备执行的软件层 |
| Stream | 保持任务顺序的一条异步执行队列 |
| Event | 用于记录进度、计时或建立不同 Stream 依赖的同步对象 |

一个算子不一定只对应一个 Kernel。例如 LayerNorm 可能被拆成多个 Kernel，也可能与相邻算子融合成一个 Kernel。反过来，一个融合 Kernel 也可能承载多个上层算子的语义。

## 01-2｜性能指标与时间线

::: info 本单元目标
围绕 **性能指标与时间线** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

不同场景中的“快”并不相同。

#### 4.1 延迟 Latency

完成一次请求或一次训练 Step 需要多久。

推理常见指标：

- **TTFT**：从请求到第一个 Token 的时间；
- **TPOT**：生成阶段相邻 Token 的平均时间；
- **E2E Latency**：整个请求从开始到结束的时间。

#### 4.2 吞吐 Throughput

单位时间完成多少工作，例如：

- Tokens/s；
- Samples/s；
- Steps/hour；
- Requests/s。

增大 Batch 往往提高吞吐，却可能增加单个请求排队时间和延迟。因此，脱离业务目标谈“性能提升”没有意义。

#### 4.3 扩展效率 Scaling Efficiency

假设单卡吞吐为 $T_1$，$N$ 卡吞吐为 $T_N$：

$$
Speedup(N)=\frac{T_N}{T_1}
$$

$$
ScalingEfficiency(N)=\frac{T_N}{N\times T_1}
$$

若单卡为 100 tokens/s，8 卡为 680 tokens/s：

$$
Speedup=6.8,\qquad ScalingEfficiency=\frac{680}{800}=85\%
$$

多卡吞吐提高了 6.8 倍，但没有达到理想的 8 倍。损失可能来自通信、同步、负载不均衡或额外数据处理。

#### 4.4 利用率 Utilization

利用率描述某种资源被使用的程度，但必须说明是哪种资源：

- 计算单元利用率；
- HBM 带宽利用率；
- 网络带宽利用率；
- 设备忙碌时间占比；
- 模型 FLOPs 利用率。

“设备利用率 100%”不等于“程序已经最优”。设备可能一直在执行低效的小 Kernel，也可能大量时间都在搬数据。

### 5. 一次任务的时间花在哪里

最粗略地看，一次任务可能包含：

$$
T_{total}=T_{host}+T_{compute}+T_{memory}+T_{comm}+T_{sync}+T_{idle}
$$

实际执行中部分阶段可以重叠，所以不能总把各项简单相加。更准确的做法是观察时间线上的依赖关系和关键路径。

![串行执行与计算通信重叠为何产生不同总时延](/images/ai-infra/performance-timeline-overlap.svg)

图中即使通信量完全相同，只要把通信藏到计算之后，总时延就会下降。性能分析不仅要统计“做了多少”，还要判断“它是否暴露在关键路径上”。

#### 5.1 关键路径

关键路径是决定任务最早完成时间的最长依赖链。优化不在关键路径上的操作，可能让局部指标变好，却不能缩短总时间。

例如：

- 某次通信耗时 10 ms，但被 15 ms 计算完全覆盖，它不是当前首要瓶颈；
- 某个 Host 同步只有 2 ms，却阻断所有后续 Kernel，它可能直接增加 2 ms 总时延；
- 某 Rank 比其他 Rank 慢 8 ms，Collective 会让所有 Rank 等待它。

## 01-3｜计算、访存与通信瓶颈

::: info 本单元目标
围绕 **计算、访存与通信瓶颈** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

#### 6.1 Compute-bound：计算受限

计算单元已接近有效峰值，继续减少访存未必能显著加速。常见于较大的、形状友好的 GEMM。

典型证据：

- 矩阵计算单元忙碌；
- 有效 FLOPs/s 较高；
- HBM 带宽未必达到峰值；
- 增加理论带宽对耗时改善有限。

常见优化方向：更好的 Tiling、提高并行度、使用低精度计算单元、改善矩阵 Shape。

#### 6.2 Memory-bound：访存受限

计算单元在等待数据，性能主要受 HBM 或其他存储层带宽限制。Norm、激活函数、逐元素操作以及 Decode 中读取大量权重，常容易受到带宽影响。

典型证据：

- HBM 带宽接近设备有效上限；
- 计算利用率不高；
- 每读写很多字节只完成少量 FLOPs；
- 融合算子、减少中间写回后明显加速。

常见优化方向：算子融合、数据复用、连续访存、减少布局转换、量化。

#### 6.3 Communication-bound：通信受限

多设备执行中，大量时间花在设备间传输、同步或等待慢 Rank。

典型证据：

- Collective 位于关键路径且暴露时间长；
- 增加卡数后扩展效率快速下降；
- 链路带宽不足或小消息启动开销突出；
- Rank 间到达 Collective 的时间差很大。

常见优化方向：通信计算重叠、更合适的 Collective 算法、增大消息聚合粒度、分层通信、改善负载均衡。

::: warning 三类瓶颈不是互斥标签
同一次训练可能在 GEMM 阶段 Compute-bound，在 Norm 阶段 Memory-bound，在梯度同步阶段 Communication-bound。必须针对具体时间段和算子下结论。
:::

## 01-4｜数量级、算术强度与峰值

::: info 本单元目标
围绕 **数量级、算术强度与峰值** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

在打开 Profiling 工具前，可以先做三个粗估算。

#### 7.1 计算时间下界

$$
T_{compute}^{min}=\frac{FLOPs}{Peak\ FLOPs/s}
$$

假设一个算子需要 20 TFLOPs，设备理论峰值为 200 TFLOPs/s：

$$
T_{compute}^{min}=\frac{20}{200}s=100ms
$$

这是理想下界。真实程序通常达不到理论峰值。

#### 7.2 访存时间下界

$$
T_{memory}^{min}=\frac{Bytes}{MemoryBandwidth}
$$

若需要从 HBM 搬运 120 GB，有效带宽为 1.2 TB/s：

$$
T_{memory}^{min}=100ms
$$

#### 7.3 通信时间下界

简单的延迟—带宽模型为：

$$
T_{comm}\approx\alpha\times R+\frac{Bytes}{EffectiveBandwidth}
$$

- $\alpha$：一次通信步骤的固定启动与同步代价；
- $R$：通信轮次；
- `Bytes`：实际经过链路的数据量；
- `EffectiveBandwidth`：考虑拓扑和协议后的有效带宽。

小消息通常更容易被 $\alpha$ 主导，大消息更容易被带宽项主导。这也是通信库需要根据消息大小选择不同算法的原因之一。

### 8. 算术强度：连接计算与访存

算术强度 Arithmetic Intensity 定义为：

$$
AI=\frac{FLOPs}{Bytes\ moved}
$$

它表示每搬运一个 Byte 数据完成多少次计算。

- AI 低：数据刚搬来只算几下，更可能 Memory-bound；
- AI 高：同一份数据被反复计算，更可能 Compute-bound。

下一阶段会专门学习 Roofline Model。现在先记住：**性能不仅由 FLOPs 决定，也由为了完成这些 FLOPs 必须搬多少数据决定。**

### 9. 为什么理论峰值不能直接代表实际性能

硬件规格中的峰值往往依赖理想条件：

- 特定数据类型；
- 足够大的规则矩阵；
- 完美的数据对齐；
- 没有依赖和气泡；
- 数据已经及时到达计算单元；
- 所有并行单元都获得均匀任务。

真实模型可能受到以下损失：

- Shape 太小或维度不友好；
- Kernel Launch 过于频繁；
- 数据布局转换；
- 分支和 Padding；
- Pipeline 气泡；
- HBM、片上 Buffer 或网络等待；
- 不同 Rank 负载不均衡。

所以性能优化的任务，是解释“理论能力为什么没有转化为有效吞吐”。

## 01-5｜性能分析流程与常见误区

::: info 本单元目标
围绕 **性能分析流程与常见误区** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

![从性能现象到根因验证的分析漏斗](/images/ai-infra/performance-analysis-funnel.svg)

#### 第一步：定义问题和指标

错误示例：“训练有点慢。”

更好的描述：“模型在 8 卡下平均 Step Time 为 2.4 s，目标是 2.0 s；与 4 卡相比，扩展效率从 91% 降到 76%。”

明确：

- 优化对象；
- 工作负载和输入 Shape；
- 硬件与软件版本；
- 基准值和目标值；
- 关注延迟、吞吐、成本还是扩展效率。

#### 第二步：建立可复现基线

固定模型、Batch、Sequence Length、精度、设备数、预热次数和测量区间。不要把第一次编译、数据加载或缓存预热混入稳定运行时间。

#### 第三步：看端到端时间线

先看整体，不要一开始钻进某个 Kernel：

- Host 是否频繁空等；
- Kernel 之间是否有大空隙；
- 通信是否位于关键路径；
- 不同 Rank 是否同步到达；
- 是否存在周期性抖动。

#### 第四步：缩小到阶段和算子

把训练 Step 拆成数据加载、前向、反向、通信、优化器，再定位占比最大的阶段和算子。

#### 第五步：提出可以验证的假设

例如：

- “这个算子受 HBM 带宽限制”；
- “小 Kernel 过多导致 Launch Gap”；
- “Rank 3 数据处理慢，使其他 Rank 在 AllReduce 等待”；
- “通信没有与反向计算重叠”。

#### 第六步：用对照实验验证

一次只改变一个主要变量，例如 Batch、消息大小、设备数、数据类型或是否开启融合。若多个变量一起变化，就很难知道收益来自哪里。

#### 第七步：验证端到端收益

局部 Kernel 加快不等于任务加快。最终必须重新测量端到端指标，并检查正确性、稳定性和资源占用是否退化。

### 11. 性能分析中的常见错误

1. **只看平均值**：平均值会掩盖 P99 抖动和个别慢 Rank。
2. **只看算子耗时排序**：高占比算子不一定有优化空间，也不一定在关键路径上。
3. **把相关性当因果**：网络利用率高与通信慢同时出现，不代表带宽一定是唯一根因。
4. **混用不同测试条件**：Batch、Shape、精度或版本不同的结果不能直接比较。
5. **忽略预热与编译**：首次运行开销与稳态性能是两个问题。
6. **只优化局部**：一个 Kernel 快了，却引入额外布局转换或同步，总时间可能更慢。
7. **只看单 Rank**：集合通信性能由整个通信组和最慢参与者共同决定。
8. **没有正确性检查**：低精度或异步优化可能提高速度，却造成数值错误或竞态。

### 12. 与后续课程的关系

本课建立的三个问题会贯穿后面所有内容：

#### 数据在哪里？

《单卡执行系统》第 2～4 章会深入设备计算单元、HBM、片上 Buffer、Tiling 和流水线；第 7 章会解释 Stream 与 Event 如何控制数据何时可用。

#### 数据有多少？

第 5 章用 Roofline 比较 FLOPs 与 Bytes；《并行策略》第 5～9 章根据并行策略计算激活、参数和梯度分片；《集合通信》第 1～5 章计算集合通信量。

#### 谁在等待？

Runtime 课程分析任务依赖，推理课程分析调度排队，分布式课程分析慢 Rank 和通信暴露，HCCL 课程最终定位到算法、Transport 与链路。

## 本章总结

> 返回：[第 1 章首页](#)

### 把本章串成一条主线

请先合上各单元正文，沿着核心概念画出输入、处理过程和输出；遇到断点时，再回到对应单元查阅。

### 13. 动手练习

#### 练习 1：定义一个性能问题

从工作中选择一个“慢”的场景，用以下模板重新描述：

```text
工作负载：
硬件与设备数：
输入 Shape / 消息大小：
当前指标：
目标指标：
测量区间：
```

#### 练习 2：计算扩展效率

某训练任务单卡吞吐 1200 tokens/s，2 卡为 2250，4 卡为 4200，8 卡为 7200。分别计算 Speedup 和 Scaling Efficiency，并观察哪次扩容损失最大。

#### 练习 3：比较理论下界

某操作需要 10 TFLOPs、搬运 40 GB 数据。设备峰值为 200 TFLOPs/s，HBM 有效带宽为 1 TB/s。分别计算计算和访存时间下界，并初步判断瓶颈。

### 14. 自测题

1. AI Infra 与模型算法关注点有什么不同？
2. 算子和 Kernel 有什么区别？
3. Runtime 主要负责哪些工作？
4. Latency、Throughput 和 Scaling Efficiency 分别描述什么？
5. 为什么设备利用率 100% 不代表程序已最优？
6. 什么是关键路径？
7. Compute-bound、Memory-bound、Communication-bound 各自受什么限制？
8. 算术强度的定义是什么？
9. 小消息通信为什么更容易受固定延迟影响？
10. 为什么局部 Kernel 加速后必须重新测量端到端指标？

::: details 自测答案

1. 模型算法关注要计算的函数与学习目标；AI Infra 关注这些计算如何在硬件和集群上高效、稳定、经济地执行。
2. 算子是上层计算语义，Kernel 是它针对特定硬件、Shape、数据类型和布局的底层实现；二者不一定一一对应。
3. 管理设备内存、Stream、Event、任务下发、同步以及 Host 与 Device 之间的协作。
4. Latency 是完成一次任务的时间；Throughput 是单位时间完成的工作量；Scaling Efficiency 衡量多设备吞吐相对理想线性增长的比例。
5. 它可能只表示设备一直有任务执行，但这些任务可能 Shape 很小、计算效率低或主要在搬运数据。
6. 决定任务最早完成时间的最长依赖链；只有缩短关键路径，端到端时间才一定下降。
7. 分别受计算单元吞吐、存储系统带宽和设备间通信/同步能力限制。
8. $AI=FLOPs/Bytes\ moved$，表示每搬运一个 Byte 完成多少次计算。
9. 总时间中的启动、同步等固定成本不会随消息字节数同比下降，小消息的数据传输时间很短，固定成本占比因而更大。
10. 局部优化可能不在关键路径上，也可能引入额外转换、同步或资源竞争，只有端到端测量才能确认真实收益。

:::

### 15. 本课小结

- AI Infra 连接模型、框架、编译、Runtime、算子、通信库和硬件；
- 一行模型代码要经过多层软件，最终才成为设备上的计算与搬运任务；
- 性能目标必须明确是延迟、吞吐、扩展效率、成本还是稳定性；
- 性能问题可初步分为计算、访存和通信受限，但结论必须针对具体阶段；
- FLOPs、搬运字节数和通信轮次可以帮助建立理论下界；
- 时间线和关键路径比孤立的算子耗时更能解释端到端性能；
- 正确流程是定义指标、建立基线、观察全局、提出假设、对照验证、确认端到端收益。

### 参考资料

- [PyTorch Profiler](https://pytorch.org/tutorials/recipes/recipes/profiler_recipe.html)
- [Roofline: An Insightful Visual Performance Model](https://dl.acm.org/doi/10.1145/1498765.1498785)
- [NVIDIA Nsight Systems User Guide](https://docs.nvidia.com/nsight-systems/UserGuide/)
- [昇腾社区文档](https://www.hiascend.com/document)

下一课将进入硬件基础：比较 CPU、GPU 和 NPU 的执行方式，并理解昇腾 AI Core 中矩阵与向量计算单元各自承担什么工作。

---

<!-- chapter-navigation -->
