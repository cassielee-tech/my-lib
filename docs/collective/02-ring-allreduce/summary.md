# 第 2 章总结｜Ring AllReduce 完整推导

> 返回：[第 2 章首页](../02-ring-allreduce.md)

## 把本章串成一条主线

请先合上各单元正文，沿"为什么是环 → RS 轮转 → AG 轮转 → 结账"复述整章；遇到断点时，再回到对应单元查阅。

```text
环形拓扑（02-1）：无中心瓶颈 · N 条链路同时满载
        ↓
RS 阶段（02-2）：N−1 轮"累积块右移"，边传边加
   → 每人一块完全规约块
        ↓
AG 阶段（02-3）：N−1 轮"完全块传阅"，纯转发
   → 每人集齐全部块 = AllReduce ✓（恒等式落地）
        ↓
账本（02-4）：T ≈ 2(N−1)α + 2S(N−1)/(N·BW)
   带宽项 ≤ 2S 与 N 无关（魔法）· 延迟项线性膨胀（诅咒）
   大消息天选 · 小消息噩梦 → 第 3 章
```

## 9. 动手练习

### 练习 1：完整手推

8 rank、数据切 8 块。写出 RS 阶段第 1～3 轮每人发送的块编号（约定：第 t 轮 rank r 发块 (r−t) mod N），并验证第 7 轮结束后每人的完全块编号。

### 练习 2：算两笔账

S=512 MB、BW=25 GB/s、α=25 µs。分别计算 N=8 与 N=64 时 Ring AllReduce 的延迟项、带宽项与总时间（切 N 块）。哪一项随 N 变化更危险？

### 练习 3：判断适用性

三个场景分别该不该用 Ring：① 1 KB 梯度同步、N=8；② 512 MB 梯度同步、N=8；③ 512 MB 梯度同步、N=1024（跨 128 台机器）。不合适的场景换什么思路？

## 10. 自测题

1. 环形拓扑相比星形的两个关键优势是什么？
2. "环"是物理连线还是逻辑编排？
3. RS 阶段每一轮的规则是什么？总共几轮？
4. 一块数据为什么恰好需要 N−1 跳才能成为"完全块"？
5. RS 结束时每个 rank 手里是什么？
6. AG 阶段的规则与 RS 有什么不同？
7. 写出 Ring AllReduce 的时间公式。
8. "带宽项与 N 无关"的确切含义和物理原因是什么？
9. Ring 在什么场景下是坏选择？为什么？
10. 2N 分块变体改变了什么、没改变什么？

::: details 自测答案

1. 无中心瓶颈（每 rank 恒定 2 条链路）；全员同时收发，N 条链路并行满载。
2. 逻辑编排：通信库让数据沿一个逻辑顺序流经所有 rank；物理网络通常是交换式的，能否支撑看拓扑（第 5 章）。
3. 每人把一块"累积块"发给右邻居，同时从左邻居收一块加进自己的同名块；共 N−1 轮。
4. 一块每被转发一次吸收一个新 rank 的贡献；自带 1 份、要吸收其余 N−1 份，故需 N−1 跳。
5. 恰好一个"完全规约块"（全组该块之和），具体哪块取决于发送方向约定。
6. AG 不做加法，纯转发"完全块"；同为 N−1 轮、每轮每 rank 收发 S/N。
7. T ≈ 2(N−1)·α + 2S(N−1)/(N·BW)。
8. 每 rank 经手字节 ≤ 2S·(N−1)/N < 2S：加 rank 只是增加并行流动的块数，不增加任何一条链路/任何一个 rank 的流量；对比星形中心需经手 N·S。
9. 小消息（α 主导）场景：2(N−1) 轮的启动成本淹没传输本身；大 N 时延迟项线性膨胀更糟。
10. 改变：chunk 粒度更细、在途流水更满、β 项兑现率更高；不变：总字节数与公式形状。

:::

## 11. 本课小结

- 环的本质：无中心 + 全员同时收发——拓扑层面的"人人平等"；
- AllReduce 的 Ring 舞蹈 = RS（边传边加，N−1 轮）+ AG（纯转发，N−1 轮）；
- 每轮每人恰好一块 S/N：带宽项 ≤ 2S 且与 N 无关，延迟项 2(N−1)α 线性膨胀；
- 大消息天选、小消息噩梦——算法选型的第一课；
- 2N 分块：同样的账本，更高的兑现率。

## 参考资料

- [Baidu Ring AllReduce（Bringing HPC Techniques to Deep Learning）](https://andrewgyork.org/high_performance_neural_networks/no_node_left_behind.html)
- [HCCL 源码专题 H01-6：Ring/RHD/NHR 算法族](../../ascend/hccl-source/06-coll-algorithms.md)
- [NCCL 官方文档](https://docs.nvidia.com/deeplearning/nccl/user-guide/index.html)
- [《模型全景》第 1 章：DDP 为什么需要 AllReduce](../../model/01-landscape/02-parallelism-communication.md)

下一课补齐另一半世界：**Tree 与 Recursive Doubling**——轮次只有 O(log N) 的算法，以及"大小消息 × rank 数"的完整选型地图。

---

<!-- chapter-navigation -->
[返回专栏目录 →](../index.md)
