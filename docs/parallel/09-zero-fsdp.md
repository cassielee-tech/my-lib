# 第 9 章｜ZeRO、FSDP 与混合并行

> 本课目标：理解 DP 三大件冗余与 ZeRO 三级分片（O → O+G → O+G+P）各自的显存/通信账；掌握 FSDP"用时拼、用完还"的参数生命周期与 prefetch 重叠；建立五把刀（DP/TP/PP/CP/ZeRO）的组合决策框架，完成本专栏收官。

## 本章导学

::: tip 本课只记住 3 件事
1. **ZeRO 的洞察**：DP 的浪费在三大件（参数/梯度/优化器状态）全员复制——按"最贵的先切"递进：**Z1 切 O（白省 60GB）、Z2 加切 G（通信 AllReduce→RS，减半）、Z3 加切 P（用时 AG 拼回，通信再涨）**。
2. **FSDP = ZeRO-3 的工程化身**：参数平时分片存、**每层用时 AG 拼回、用完立刻释放**——计算完整（不切算），只切存储；prefetch 让参数预取与当前层计算重叠。
3. **组合的口诀**：切什么取决于什么放不下——**参数放不下→TP/PP/ZeRO，序列放不下→CP，都放得下→DP**；五把刀不是竞争，是对着不同的墙。
:::

**学习节奏：** 本章拆为 **4 个学习单元，每个约 15 分钟**。账本是主角——每个 ZeRO 等级都算两笔账（显存、通信），算完等级的选择就不再是背诵题。

**跟不动？** 先读 [⚡ 速通版（约 5 分钟）](#⚡-速通-约-5-分钟)：一个"合租分摊三样东西"的类比讲完整章，再回来按单元深入。

- [ ] 我能写出 Z1/Z2/Z3 在 N 卡下的每卡显存与每步通信量
- [ ] 我能说出 FSDP 一个层的前向/反向参数生命周期
- [ ] 我能给"模型放不下/序列放不下/都放得下"三种场景各配一套并行方案

## 本章单元

- **09-1（约 15 分钟）**：[ZeRO 的洞察：三大件冗余](#_09-1-zero-的洞察-三大件冗余)
- **09-2（约 15 分钟）**：[FSDP：用时拼、用完还](#_09-2-fsdp-用时拼、用完还)
- **09-3（约 15 分钟）**：[五把刀的组合策略](#_09-3-五把刀的组合策略)
- **09-4（约 15 分钟）**：[收官：总表与地图](#_09-4-收官-总表与地图)

- **本章总结**：[练习、自测与专栏验收](#本章总结)

## 这一课在整条路线中的位置

```text
第 5-8 章：DP / TP / PP / CP —— 四把刀各就各位
        ↓
本章：ZeRO/FSDP（第五把刀：切训练状态的存储）
      + 组合策略 → 五把刀的完整方法论 → 专栏收官
        ↓
《集合通信》/《昇腾与 HCCL》：这些"需求"怎样被通信库高效供给
```

[第 5 章](05-ddp.md)的 DDP 账本（三大件全员复制）、[《集合通信》04-2](../collective/04-gather-scatter-alltoall.md#_04-2-reducescatter-规约后分着拿) 的"RS 比 AllReduce 省一半"、[《训练与推理系统》02-3](../systems/02-training-memory-compute.md#_02-3-梯度累积与-offload-腾挪空间) 的 Offload——三条线索在本章汇合。

---

[开始单元 09-1 →](#_09-1-zero-的洞察-三大件冗余)

## ⚡ 速通（约 5 分钟）

> 适用：时间紧，或完整版跟不动时。这页用"合租分摊三样东西"讲完整章，读完抓住 80% 的主干。任何一节想深入，拉到文末点对应单元。

### 1. 三样东西，八份复制：ZeRO 的洞察

8 个人合租办公，每人都买了**一整套装备**：常用工具（参数 15 GB）、每周的作业本（梯度 15 GB）、还有一座**巨型工具房**（优化器状态 60 GB）——每人 90 GB，全楼 720 GB，其中 630 GB 是一模一样的 7 份复制。

ZeRO 的灵魂拷问：**工具房真的需要每人一座吗？** 观察发现：修工具是**各修各的**（优化器更新逐参数独立）——每人只负责 1/8 的家伙，完全可以。于是三级递进，**最贵的先切**：

- **ZeRO-1（切工具房）**：每人保管 1/8 的优化器状态——显存 90→37.5 GB，**其他什么都不用改，白省**；
- **ZeRO-2（加切作业本）**：梯度也各管一段——上交作业时每人**只留自己那段**（AllReduce 换成 ReduceScatter），**运费还减半**，纯赚；
- **ZeRO-3（加切工具）**：连参数都拆开分放——要用时再拼，显存 11.25 GB，但**每次开工前都得先把工具凑齐**（AllGather 参数），运费涨到 1.5 倍——放不下才用的最后手段。

::: details 小测 1：为什么 Z2 的通信量反而比 DP 少？
梯度分片后每人只需要"自己负责那段参数"的梯度——AllReduce（每人拿全长）降级成 ReduceScatter（各留一段），流量正好减半。
:::

### 2. 用时借、用完还：FSDP

ZeRO-3 的"拼工具"听起来很笨重——FSDP 把它做成了**图书馆模式**：

- 平时：所有书拆页分放在 8 个书架（参数分片）；
- 干活：**今天要用第 3 章就把第 3 章借齐**（逐层 AllGather），**看完立刻还**（释放），桌上永远只有一章；
- 妙招（prefetch）：**读第 3 章的时候，第 4 章已经在快递路上**——借书时间被读书盖住（通信与计算重叠）。

它和 TP 的本质区别一句话：**TP 连"算"都切了**（每人算自己那块，激活也省）；**FSDP 只切"存"**（拼齐了整层完整地算，激活不省）。所以 TP 吞吐极限高但只能机内、工程重；FSDP 简单弹性、随便跨机，但通信多一点——**没有免费的刀，只有匹配的刀**。

::: details 小测 2：FSDP 的显存峰值是什么？
当前拼齐的那一层（完整）+ 全部激活——参数省了，但激活因为"计算不切"而没省。
:::

### 3. 五把刀的排班表：组合

最后把五把刀放回工具墙，**开方前先诊断——什么放不下？**

| 诊断 | 开方 |
| --- | --- |
| 都放得下，要吞吐 | DP（+Z1/Z2 免费优化） |
| 单层放不下 | TP（机内 8 卡） |
| 总体放不下、想简单 | FSDP/ZeRO-3（一行 wrap） |
| 总体放不下、要极限 | Megatron 3D（TP 机内 × PP 跨机 × DP 扩展） |
| 序列放不下 | CP（切序列） |
| MoE | EP（专家分布式） |

每个策略 = 把一种冗余换成一种通信；组合 = **让每种通信落到性格匹配的链路**（TP 给机内专线、PP 给跨机 P2P、DP 给慢网也能忍的重叠流）。收官一句话：本专栏讲清了"为什么通信"——每个需求都能在《集合通信》的供给表里找到对应的那一行。

::: details 小测 3：70B 预训练、64 机 8 卡、要极限吞吐，怎么配？
TP=8（机内贴 HCCS）× PP 跨机分段（P2P 边界激活）× DP 用剩余卡扩展；激活紧张再叠 CP——即 Megatron 3D/4D 配方。
:::

### 3 句话带走

1. **ZeRO 按最贵的先切**：O 白省、G 减半（RS）、P 要付 AG 的钱——显存 ÷N 的三级台阶；
2. **FSDP = 图书馆模式**：逐层用时拼、用完还，prefetch 把借书藏进读书——只切存储、不切计算；
3. **组合先诊断**：什么放不下决定用哪把刀；每种并行都是"冗余换通信"，让通信落到匹配的链路。

### 黑话小词典

| 术语 | 人话 |
| --- | --- |
| 三大件 | 工具（P）+ 作业本（G）+ 工具房（O，占 2/3） |
| ZeRO-1/2/3 | 逐级把 O、G、P 也拆开分放 |
| ReduceScatter | 上交作业各留一段——运费减半的原语替换 |
| FSDP | 图书馆：拆页存放、按章借还 |
| prefetch | 读第 3 章时，第 4 章已在快递路上 |
| 洋葱 wrap | 逐层包 FSDP，一次只拼一层 |
| Megatron 3D | TP 机内 × PP 跨机 × DP 扩展的极限配方 |
| Offload | 分片还嫌大，甩到 CPU/硬盘 |

### 想深入？

每节 15 分钟，按需点开，不必按顺序全读：

| 单元 | 讲什么（白话） | 什么时候需要它 |
| --- | --- | --- |
| [09-1 ZeRO 洞察](#_09-1-zero-的洞察-三大件冗余) | 三大件冗余 + 三级账本 | 要做显存/通信权衡决策 |
| [09-2 FSDP](#_09-2-fsdp-用时拼、用完还) | 参数生命周期 + prefetch + 与 TP 之辨 | 要上手 FSDP 微调 |
| [09-3 组合策略](#_09-3-五把刀的组合策略) | 决策树 + 两个配方 + 五刀总表 | 要设计训练方案 |
| [09-4 收官地图](#_09-4-收官-总表与地图) | 需求↔供给会师表 + 专栏三条带走的话 | 专栏总复习 |
| [本章总结](#本章总结) | 练习 + 10 道自测 + 专栏验收 | 检验是否真正掌握 |

## 09-1｜ZeRO 的洞察：三大件冗余

::: info 本单元目标
围绕 **"按最贵的先切的分片递进"** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

### 1. DP 到底浪费了什么

回扣 [第 5 章](05-ddp.md)：DP 每卡一份完整模型。7.5B 模型（BF16 + fp32 Adam）的三大件：

| 三大件 | 大小 | 说明 |
| --- | --- | --- |
| 参数 P（fp16） | 15 GB | 前向/反向要用 |
| 梯度 G（fp16） | 15 GB | 反向产出、更新要用 |
| 优化器状态 O（fp32 m+v） | **60 GB** | **占 2/3 的大头** |

合计 90 GB（完整混合精度账见 [《训练与推理系统》02-1](../systems/02-training-memory-compute.md#_02-1-训练显存账本-四个大件)）——8 卡 DP 总共存了 **8×90=720 GB**，其中 630 GB 是**一字不差的 7 份复制**。ZeRO（Zero Redundancy Optimizer）的问题只有一个：**这些复制必要吗？**

关键观察：**优化器更新是逐参数独立的**——`θᵢ ← θᵢ − f(mᵢ, vᵢ, gᵢ)` 只看自己的那一位。那么 N 张卡完全可以**各更新 1/N 的参数**，谁也不需要完整的 O。

### 2. 三级递进：最贵的先切

| 等级 | 分片对象 | 每卡显存（N=8） | 每步通信 | 通信量（vs DP） |
| --- | --- | --- | --- | --- |
| DP | 无 | 90 GB | AllReduce(G) ≈ 26 GB | 1× |
| **ZeRO-1** | O | 15+15+7.5 = **37.5 GB** | 不变（G 仍全量 AllReduce） | 1× |
| **ZeRO-2** | O + G | 15+1.9+7.5 = **24.4 GB** | **ReduceScatter(G)** ≈ 13 GB | **0.5×** ✅ |
| **ZeRO-3** | O + G + P | 90/8 = **11.25 GB** | RS(G) + **AG(P)×前向/反向** | ~1.5× |

两级账本读懂设计：

**Z1 是"白省"**：O 分片后每卡只更新自己的参数段——**计算与通信都不变，显存直接砍掉 (N−1)O/N**。没有理由不做。

**Z2 的通信魔法**：梯度分片后，AllReduce 变 **ReduceScatter**——每人只需要"自己那段参数"的梯度，为什么要拿全长？（[《集合通信》04-2](../collective/04-gather-scatter-alltoall.md#_04-2-reducescatter-规约后分着拿) 的 FSDP 省钱逻辑，回扣 [01-4 的"最小够用原语"](../collective/01-collective-semantics-cost.md#_01-4-从语义到选型)）。**通信减半，纯赚**。

**Z3 的代价**：参数也分片后，前向/反向**用哪层就得先把哪层拼齐**（AllGather）——通信从 0.5× 涨回 1.5×。它换来的是"**任意大模型都能放下**"——这是用通信换显存的最后一档。

::: tip 选择的直觉
Z1/Z2 几乎免费，默认开；Z3 是"放不下才用"的最后手段——而下一段的 FSDP 会告诉你，Z3 的"拼参数"也可以做得很优雅。
:::

## 09-2｜FSDP：用时拼、用完还

::: info 本单元目标
围绕 **"参数的生命周期：分片存放、按层拼回、用完释放"** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

FSDP（Fully Sharded Data Parallel）= ZeRO-3 思想的 PyTorch 工程化。核心问题是：参数分片了，前向/反向要完整参数怎么办？

### 3. 洋葱式包一层，一次只拼一层

FSDP 的答案：**不拼整个模型，逐层"用时拼、用完还"**。

对每个 Transformer 层包一层 FSDP（nested wrap），它的参数生命周期：

```text
平时：    参数分片存放（每卡 1/N），不占整层显存
前向该层：AllGather(Pᵢ) → 拼齐 → 计算 → 立刻释放，只留分片
反向该层：AllGather(Pᵢ) → 拼齐 → 算梯度 → ReduceScatter(Gᵢ) → 释放
更新：    各卡只更新自己那段（Z1 的逻辑）
```

显存峰值 = **当前拼齐的那一层 + 全部激活**（激活不省——这是"计算不切、只切存储"的代价，与 TP 的本质区别见下表）。

| | TP（[第 6 章](06-tensor-parallel.md)） | FSDP（本章） |
| --- | --- | --- |
| 参数存放 | 分片 | 分片 |
| **计算** | **分片**（每卡算自己的块） | **完整**（拼齐了整层算） |
| 激活 | 随分片 ÷N | 完整（不省） |
| 通信 | 激活级、每层 4 次、不可重叠 | 参数级、每层 3 次、**可预取重叠** |

### 4. Prefetch：把拼参数藏进计算

逐层 AG 的通信在关键路径上吗？——**不在，因为可以预取**：算第 $i$ 层时，第 $i{+}1$ 层的参数已经在后台 AG——**通信与当前层计算重叠**（与 [第 5 章 DDP](05-ddp.md#_05-3-bucket-分桶的艺术) 的桶预取同一个思想，不同对象）。

FSDP 的使用体验（对照 [第 4 章](04-distributed-basics.md#_04-4-工程实操-跑起来与排障)的骨架）：

```python
from torch.distributed.fsdp import FullyShardedDataParallel as FSDP

model = Transformer(...)                        # 每卡先有完整定义
model = FSDP(model, auto_wrap_policy=...)       # 包洋葱 → 参数即刻分片
# 训练循环与 DDP 几乎一样：loss.backward() 内部自动完成
#   AG(P)→算→RS(G) 的逐层接力，optimizer.step() 各更新各段
```

::: warning 三条工程备注
1. **`no_sync()` + 梯度累积**：累积步内不 RS，进一步省通信（回扣 [第 5 章 05-4](05-ddp.md#_05-4-通信计算重叠-把等待藏起来)）；
2. **Offload 兜底**：分片还能再甩到 CPU/NVMe（ZeRO-Offload/Infinity，[《训练与推理系统》02-3](../systems/02-training-memory-compute.md#_02-3-梯度累积与-offload-腾挪空间) 的 Offload 思想 × 分片思想的乘法）；
3. **选择直觉**：中小模型/想要简单 → FSDP（无 stage、无调度心智）；超大模型/极限吞吐 → TP+PP 的 Megatron 路线（通信更省但工程更重）——两者也可组合。
:::

## 09-3｜五把刀的组合策略

::: info 本单元目标
围绕 **"切什么取决于什么放不下"** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

五把刀凑齐了。真实的部署问题从来不是"选哪把"，而是"**怎么配**"。

### 5. 决策树：先诊断，再开方

```text
模型放得下单卡吗？
├─ 放得下 ──► DP（+ ZeRO-1/2：几乎免费的优化）
└─ 放不下 ──► 放不下的原因是？
    ├─ 单层就放不下 ────────► TP（机内 8 卡，层内切分）
    ├─ 单层放得下、总体放不下 ─► FSDP/ZeRO-3（切存储）
    │                          或 PP（切深度，跨机）
    ├─ 激活/KV 放不下 ────────► CP（切序列，[第 8 章](08-context-parallel.md)）
    └─ 是 MoE ───────────────► EP（专家分布式，[第 1 章](01-moe-expert-parallel.md)）
```

诊断的顺序就是开方的顺序：**先问显存墙在哪，再选刀**——这也是 [08-4](08-context-parallel.md#_08-4-cp-的账与-4d-组合) "切什么取决于什么放不下"的完整版。

### 6. 两个经典配方

**配方 A：Megatron 3D（极限吞吐）**——大厂预训练的标配：

```text
TP=8（机内）× PP=8~16（跨机）× DP（其余卡） [+ CP 长序列] [+ EP 若 MoE]
```

通信全部按性格归位：TP 贴机内专线、PP 走相邻 P2P、DP 低频可重叠——代价是配置复杂、负载要人工配平。

**配方 B：FSDP 系（简单弹性）**——中小团队与微调场景：

```text
ZeRO-2/3（+ Offload）× DP —— 一行 wrap，云上弹性伸缩
```

通信多一点、吞吐逊一点，换来工程上的省心——**没有最好的并行，只有与场景匹配的并行**。

#### 6.1 五把刀总表（背这张就够）

| 刀 | 切什么 | 通信对象/形态 | 通信性格 | 显存收益 | 位置约束 |
| --- | --- | --- | --- | --- | --- |
| DP | batch | 梯度 / AllReduce | 大而低频，可重叠 | 无（ZeRO 补） | 任意 |
| ZeRO | 存储状态 | G 的 RS + P 的 AG | 中频，可预取重叠 | 三大件 ÷N | 任意 |
| TP | head/特征 | 激活 / AG·RS·AR | 小而高频，不可重叠 | 三大件+激活 ÷N | **机内** |
| PP | 深度 | 边界激活 / P2P | 极小，有 Bubble | 三大件 ÷S | 跨机 ✅ |
| CP | 序列 | K/V / AG 或环传 | 与 S 成正比，Ring 可重叠 | 激活+KV ÷N | 跨机可行 |

::: tip 组合的本质
五把刀各自把**一种冗余或一堵墙**变成通信——组合 = 让每种通信都落到性格匹配的链路上（[第 4 章](04-distributed-basics.md#_04-3-拓扑感知排布-把编号贴到物理上)的拓扑感知排号把它们钉在物理上）。
:::

## 09-4｜收官：总表与地图

::: info 本单元目标
围绕 **"五把刀与通信需求的完整地图"** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

### 7. 需求方与供给方的会师

本专栏回答"**为什么通信**"——现在把全部需求列成一张清单，去《集合通信》对账：

| 并行策略 | 产生的通信 | 《集合通信》对应 |
| --- | --- | --- |
| DP/DDP | 梯度 AllReduce（逐桶） | 第 2 章 Ring 的主战场 |
| ZeRO-2/3 | G 的 RS、P 的 AG | 第 4 章两大单原语 |
| TP | 每层 AG/RS/AllReduce | 第 4 章 + 第 5 章分层 |
| PP | 边界激活 P2P | 第 1 章原语家族的 Send/Recv |
| CP | K/V 的 AG 或环传 | 第 5 章 Chunk 流水同构 |
| EP（MoE） | Token 的 AlltoAll | 第 4 章失衡专题 |
| 所有策略 | 通信计算重叠 | 第 5 章 05-4 |

**每一行都能对上**——这就是两条线的设计意图：并行策略是需求侧的故事，集合通信是供给侧的故事，本专栏收官处二者会师。

### 8. 专栏收官：三条带走的话

1. **通信是算法的一部分**：从 DP 的"必须取平均"到 CP 的"必须看到 K/V"——并行策略先定义正确性，再谈效率；
2. **每种并行 = 把一种冗余换成一种通信**：DP 换吞吐、ZeRO 换显存、TP 换层内算力、PP 换跨机扩展、CP 换序列容量——**没有免费的刀**；
3. **组合 = 让通信落到性格匹配的链路**：机内给 TP、跨机给 PP/CP、慢网给可重叠的 DP——拓扑感知（第 4 章）是最后一颗钉子。

下一站的选择：

- 补理论 → [《集合通信》](../collective/index.md)：把需求清单里的每个原语推到底；
- 落平台 → [《昇腾与 HCCL》](../ascend/index.md)：torch_npu → HCCL 的调用链与源码（[HCCL 源码 6](../ascend/hccl-source/06-allreduce-call-chain.md) 正是 DDP 那一行 `all_reduce` 的下半生）。

## 本章总结

> 返回：[第 9 章首页](#)

### 把本章串成一条主线

请先合上各单元正文，沿"冗余 → 递进 → 工程化 → 组合 → 会师"复述整章；遇到断点时，再回到对应单元查阅。

```text
洞察（09-1）：DP 三大件 8 份复制 · 更新逐参数独立 → 分片合法
   Z1 切 O（白省）→ Z2 加切 G（AR→RS，通信减半）
   → Z3 加切 P（用时 AG，通信 1.5×，显存 ÷N）
        ↓
FSDP（09-2）：Z3 的工程化身 —— 逐层"用时拼、用完还"
   计算完整不切 · 激活不省 · prefetch 预取重叠
   Offload 兜底 · 与 TP 的"切不切计算"之辨
        ↓
组合（09-3）：先诊断再开方 —— 放不下的是参数/序列/还是都放得下？
   Megatron 3D（极限吞吐）vs FSDP 系（简单弹性）· 五把刀总表
        ↓
会师（09-4）：需求清单 ↔ 《集合通信》供给表逐行对上 —— 专栏收官
```

### 9. 动手练习

#### 练习 1：三级账本

7.5B（P=15、G=15、O=60 GB）、N=16。填写 DP/Z1/Z2/Z3 的每卡显存与每步通信量（RS=G(N-1)/N、AG(P) 按前向+反向两趟计），并指出哪一级开始"通信变贵"。

#### 练习 2：生命周期排序

把 FSDP 中一个 Transformer 层在一步内的事件按时间排序：AG(Pᵢ)前向、释放、AG(Pᵢ)反向、RS(Gᵢ)、本段更新、预取 AG(Pᵢ₊₁)——标出预取与哪段计算重叠。

#### 练习 3：开方

三个场景各配一套方案：① 7B 微调、8 卡单机、显存够但想快；② 70B 预训练、64 机 8 卡、追求极限吞吐；③ 128K 长序列推理服务、KV Cache 撑爆单卡。写出刀的选择与理由。

### 10. 自测题

1. 三大件各多大？为什么说优化器状态是大头？
2. "优化器更新逐参数独立"这一观察为什么让分片合法？
3. Z1 为什么说几乎免费？
4. Z2 的通信为什么减半？用到什么原语替换？
5. Z3 的通信代价是什么？换来什么？
6. FSDP 一层的参数生命周期四个阶段是什么？
7. FSDP 与 TP 在"切不切计算"上的区别带来哪两项显存差异？
8. prefetch 重叠的是什么与什么？
9. 决策树的第一问是什么？三个分支各指向哪些刀？
10. 两种经典配方各自的取舍是什么？

::: details 自测答案

1. 7.5B：P=15 GB、G=15 GB、O=60 GB；O（fp32 m+v）占 2/3。
2. 更新 θᵢ 只依赖自己的 mᵢ、vᵢ、gᵢ——N 卡各更新 1/N 参数互不干扰，完整复制没有必要。
3. 显存砍掉 (N−1)O/N，而梯度的 AllReduce 与计算完全不变——没有代价。
4. 梯度分片后每人只要自己那段：AllReduce(G)（2G(N−1)/N）换成 ReduceScatter(G)（G(N−1)/N）——"最小够用原语"。
5. 每层前向/反向都要 AG 拼回参数，通信从 0.5× 涨到约 1.5×；换来任意大模型放得下（每卡 90/N GB）。
6. 分片存放 → AG 拼齐（前向）→ 释放 → AG 拼齐（反向）→ RS 梯度 → 释放 → 更新自己段。
7. FSDP 计算完整：激活不省（完整保存）；TP 计算分片：激活随分块 ÷N。
8. 第 i+1 层参数的 AG 与第 i 层的计算——参数预取藏进当前层前向/反向。
9. "模型放得下单卡吗？"放得下→DP(+Z1/Z2)；单层放不下→TP；总体放不下→FSDP/PP；序列放不下→CP；MoE→EP。
10. Megatron 3D：通信最优、吞吐极限，但配置复杂、负载要人工配平；FSDP 系：工程简单、弹性好，通信与吞吐略逊。

:::

### 11. 专栏验收清单

《并行策略》九讲收官。对照验收：

- [ ] 能画出 2 机 16 卡的 rank 编号表并为 TP×DP 写出组身份（[第 4 章](04-distributed-basics.md#_04-1-从进程到-rank-人口登记)）
- [ ] 能推导 DP 梯度 AllReduce 的必要性并算每步通信（[第 5 章](05-ddp.md#_05-1-dp-语义-为什么必须-allreduce)）
- [ ] 能独立推导 ColP→GeLU→RowP 的 shape 流转（[第 6 章](06-tensor-parallel.md#_06-2-column-与-row-两种切法)）
- [ ] 能画 1F1B 时间线并用 Bubble 公式估算（[第 7 章](07-pipeline-parallel.md#_07-2-micro-batch-与-bubble)）
- [ ] 能对比 CP 的 AG 与 Ring 路线并解释 causal 三角（[第 8 章](08-context-parallel.md#_08-2-attention-的通信-allgather-还是-ring)）
- [ ] 能填 Z1/Z2/Z3 的显存与通信账并按决策树开方（本章）
- [ ] 能把六种并行的通信需求对到《集合通信》的原语供给表（本章 09-4）

### 参考资料

- [ZeRO 论文](https://arxiv.org/abs/1910.02054) 与 [FSDP 论文](https://arxiv.org/abs/2304.11277)
- [《训练与推理系统》02 章：显存账本与 Offload](../systems/02-training-memory-compute.md#_02-1-训练显存账本-四个大件)
- [《集合通信》04-2：ReduceScatter 的省钱逻辑](../collective/04-gather-scatter-alltoall.md#_04-2-reducescatter-规约后分着拿)
- [《集合通信》05-4：重叠的条件与边界](../collective/05-topology-hierarchical-overlap.md#_05-4-通信计算重叠-从公式到实践)
- [HCCL 源码 6：AllReduce 调用链](../ascend/hccl-source/06-allreduce-call-chain.md)

---

<!-- chapter-navigation -->
