# 第 5 章｜数据并行与 DDP

> 本课目标：从"为什么不同步梯度就会发散"推出 DP 的通信必然性；算清 DDP 每步的通信账；理解 Bucket 分桶与通信计算重叠各自的动机与权衡；能手写朴素 DDP 并说清生产级 DDP 多做了什么。

## 本章导学

::: tip 本课只记住 3 件事
1. **DP = 完整模型复制 + 数据切片 + 梯度取平均**：各卡看到不同数据、梯度必然不同，不做 AllReduce 就是 N 个各自漂移的模型——通信是数学正确性的一部分，不是性能优化。
2. **Bucket 分桶是粒度的折中**：按单参数通信 → 小消息 α 主导；全量一次通信 → 无法与反向重叠——桶"攒一批再发"，平衡两头。
3. **重叠的前提天然成立**：通信的是已算完的梯度、依赖它的是反向结束后的 `optimizer.step()`——中间整段反向都可以与通信并行，双流时间线把 M 秒藏进 N 秒。
:::

**学习节奏：** 本章拆为 **4 个学习单元，每个约 15 分钟**。本章计算多、代码少，建议拿计算器跟着算——通信账算一遍，DDP 就没有黑盒了。

**跟不动？** 先读 [⚡ 速通版（约 5 分钟）](#⚡-速通-约-5-分钟)：一个"连锁分店对账"的类比讲完整章，再回来按单元深入。

- [ ] 我能解释"不做梯度 AllReduce 会发生什么"
- [ ] 我能算出给定模型/卡数/带宽下 DDP 每步的通信时间
- [ ] 我能画出"反向 + 逐桶通信"的双流重叠时间线

## 本章单元

- **05-1（约 15 分钟）**：[DP 语义：为什么必须 AllReduce](#_05-1-dp-语义-为什么必须-allreduce)
- **05-2（约 15 分钟）**：[从朴素 DDP 到生产级 DDP](#_05-2-从朴素-ddp-到生产级-ddp)
- **05-3（约 15 分钟）**：[Bucket：分桶的艺术](#_05-3-bucket-分桶的艺术)
- **05-4（约 15 分钟）**：[通信计算重叠：把等待藏起来](#_05-4-通信计算重叠-把等待藏起来)

- **本章总结**：[练习、自测与串联](#本章总结)

## 这一课在整条路线中的位置

```text
第 4 章：world group 上的编号与分组
        ↓
本章：world group 上的第一次集体行动——梯度 AllReduce
        ↓
第 6-9 章：TP / PP / CP / ZeRO（通信从"步间"走进"层间"）
```

模型侧的叙事在 [《模型全景》02-4](../model/02-tensor-autograd.md#_02-4-梯度-shape-与集合通信)（梯度为什么需要通信）与 [《训练与推理系统》01-4](../systems/01-training-loop.md#_01-4-训练显存与梯度通信)（通信发生在反向与更新之间）已埋好——本章把两句"预告"变成完整的推导与账本。HCCL 侧的对应物是 [HCCL 源码 6：AllReduce 调用链走读](../ascend/hccl-source/06-allreduce-call-chain.md)。

---

[开始单元 05-1 →](#_05-1-dp-语义-为什么必须-allreduce)

## ⚡ 速通（约 5 分钟）

> 适用：时间紧，或完整版跟不动时。这页用"连锁分店对账"讲完整章，读完抓住 80% 的主干。任何一节想深入，拉到文末点对应单元。

### 1. 分店与强制对账：为什么必须 AllReduce

你开了 8 家**连锁分店**（8 张卡），每家都有完整菜单（完整模型副本），各自接待自己的客人（不同的数据分片）。

一个月下来各算各的利润（各自的梯度）——问题来了：**如果不统一**，下个月 8 家店的进货策略（参数更新）就各走各路，连锁店当场散伙。所以月底必须做一件事：**8 家利润求平均，全体按平均值统一进货**——这个动作就是梯度 AllReduce。

记住这句：**不做 AllReduce 不是"慢一点"，是训练在数学上直接错了**。所以 DP 的通信是算法本身的一步，不是性能优化。

账本有多厚？梯度 ≈ 2×参数量（BF16）——7.5B 模型就是 15 GB，每卡过手约 2×15×7/8 ≈ 26 GB（Ring 账本）。好消息：这笔账**跟 batch 大小无关**——batch 越大、算得越久，同样的运费被摊得越薄，这就是 DP 好扩展的原因。

::: details 小测 1：为什么说 DP 的通信"不是性能问题"？
因为缺少 AllReduce 时各副本会漂移成 N 个不同模型，训练结果在数学上就是错的——它是算法（等价大 batch SGD）的组成部分。
:::

### 2. 账本分章寄：Bucket

对账的正确姿势不是"月底一次性寄 15 GB"——那样要等所有账算完（反向结束）才能寄，寄的那段时间全店停工。

也不是"记一笔寄一笔"——几万笔快递，每笔的起步价（延迟 α）能把人逼疯。

**分章寄**：把账本按记账完成顺序分成若干章（**Bucket，每章约 25 MB**）——这一章记完，快递立刻出发；记账员继续记下一章。章的大小是个旋钮：太大 → 拖到最后才能寄；太小 → 快递太碎、起步价占比飙升。这就是分桶的两端权衡。

::: details 小测 2：桶为什么按"反向完成顺序"装，而不是按模型定义顺序？
反向是从输出层往输入层走的——先算完的梯度装进先出发的桶，通信才能尽早开始；按定义顺序装桶会让"早就算完的梯度"干等"晚才算完的同伴"。
:::

### 3. 边记账边寄：重叠

现在把两条流水线摆在一起：

```text
记账员（计算流）：  [记第3章][记第2章][记第1章]══╗ → 统一进货(step)
                            └已寄┘ └已寄┘      ║
快递员（通信流）：  ·····[等第1章]→寄→[等第2章]→寄→╝
```

**记账员记后面几章时，前面几章已经在路上**——串行的"记账 10 秒 + 寄账 1 秒"变成约 10 秒。能做到的原因：寄的是**已经记完的章**、等货的只有月底统一进货（step）——中间整段时间天然空闲。

但有三样藏不住：①快递比记账还慢时（小模型+慢网），多出来的照样堵；②"统一进货"前必须等最后一章到货（语义下界）；③如果本月记 K 次账才对一次账（**梯度累积**），运费直接除以 K——免费的省钱。

::: details 小测 3：反向 4 秒、通信流忙 9 秒、一步 10 秒——瓶颈在哪？
通信。通信已经从第 1 秒就在飞（重叠生效），但 9 秒 > 4 秒，裸奔的 5 秒藏不住——出路：压通信量（量化/压缩）、换算法、或上第 9 章的 ZeRO/FSDP。
:::

### 3 句话带走

1. **DP = 完整模型 + 数据切片 + 梯度求平均**：不做 AllReduce 就是分店散伙，数学错误；
2. **Bucket = 账本分章寄**：按反向顺序装桶、桶满即发，避开"太碎（α）"与"太整（不能重叠）"两个极端；
3. **重叠 = 记账寄货双流水线**：串行 N+M 变 max(N,M)；藏不住的部分靠压量、换算法或梯度累积。

### 黑话小词典

| 术语 | 人话 |
| --- | --- |
| DP / DDP | 数据并行 / 它的生产级实现（多进程版） |
| AllReduce(avg) | 全员求和取平均——分店强制统一 |
| Bucket | 账本的一章：约 25 MB，记完即寄 |
| bucket_cap_mb | 章有多厚的旋钮 |
| 反向 hook | 记账员"记完一章喊一声"的机制 |
| 双流重叠 | 记账员与快递员同时干活 |
| 梯度累积 | 记 K 次账才对一次账，运费÷K |

### 想深入？

每节 15 分钟，按需点开，不必按顺序全读：

| 单元 | 讲什么（白话） | 什么时候需要它 |
| --- | --- | --- |
| [05-1 DP 语义](#_05-1-dp-语义-为什么必须-allreduce) | 发散推导 + 每步通信账 | 要理解"为什么必须通信" |
| [05-2 朴素到 DDP](#_05-2-从朴素-ddp-到生产级-ddp) | 十行朴素版 + DDP 三件事 | 要读懂 DDP 代码与参数 |
| [05-3 Bucket](#_05-3-bucket-分桶的艺术) | 两端权衡 + 装桶编排 | 要调 bucket_cap_mb / 排查通信碎 |
| [05-4 重叠](#_05-4-通信计算重叠-把等待藏起来) | 双流时间线 + 三条边界 | 要分析一步的耗时构成 |
| [本章总结](#本章总结) | 练习 + 10 道自测 | 检验是否真正掌握 |

## 05-1｜DP 语义：为什么必须 AllReduce

::: info 本单元目标
围绕 **"梯度取平均的数学必然性"** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

### 1. 数据并行在切什么

与后面所有章节不同，DP **不切模型**：每张卡一份完整副本，切的是**数据**——global batch 均分为 N 份，每卡各喂一份。

```text
rank 0: 模型副本 + batch[0:B/N]     rank 1: 模型副本 + batch[B/N:2B/N]
rank 2: 模型副本 + batch[...]        rank 3: 模型副本 + batch[...]
```

前向各算各的 loss，反向各算各的梯度——到这里为止**零通信**。通信发生在哪？反向结束、更新之前。

### 2. 为什么不做 AllReduce 就会发散

关键事实：**各卡数据不同 → 各卡梯度不同**。如果各自直接更新：

```text
θ⁽⁰⁾ ← θ - lr·g⁽⁰⁾        ← rank 0 只往自己的数据方向走
θ⁽¹⁾ ← θ - lr·g⁽¹⁾        ← rank 1 走另一个方向
……
```

一步之后，N 张卡上出现 **N 个不同的模型**；下一步的数据再进来，差异继续放大——这不是"变慢"，是**训练在数学上已经错了**。

正确语义等价于"大 batch SGD"：

$$
\bar{g} = \frac{1}{N}\sum_{i=0}^{N-1} g^{(i)},\qquad \theta \leftarrow \theta - \text{lr}\cdot\bar{g}
$$

$\bar g$ 的求解动作正是 **AllReduce(avg)**（回扣 [《集合通信》01-2 原语卡片](../collective/01-collective-semantics-cost.md#_01-2-六个核心原语的语义卡片)）——所以 DP 的通信不是"性能问题"，是**算法本身的一步**。

#### 2.1 每步通信账

用 Ring 账本（[《集合通信》02-4](../collective/02-ring-allreduce.md#_02-4-ring-的账本与变体)）给 DDP 每步记账：

- 通信对象：**全量梯度** $S = 2\times$参数量（BF16 字节）；
- 每卡流量：$2S\cdot\frac{N-1}{N}$——**与 batch 大小无关**；
- 扩展性来源：batch 越大 → 反向计算越久 → 同样的通信量被越厚的计算摊薄。

**算例**：7.5B 模型、BF16、8 卡（机间 25 GB/s）：$S = 15$ GB，每卡流量 $2\times15\times\frac78 \approx 26$ GB，通信时间 $\approx 1.05$ s；batch 32×2048 token 的反向约 10 s（@300 TFLOPS）——**通信约占一步的 10%**，重叠后几乎可藏（05-4 展开）。

::: tip 位置回顾
"梯度 AllReduce 发生在反向与 `optimizer.step()` 之间"——这句在 [《训练与推理系统》01-4](../systems/01-training-loop.md#_01-4-训练显存与梯度通信) 出现过的话，现在有了完整的推导与账本。
:::

## 05-2｜从朴素 DDP 到生产级 DDP

::: info 本单元目标
围绕 **"生产级 DDP 比朴素版多做的三件事"** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

### 3. 朴素版：十行写对 DP

语义清楚后，朴实现呼之欲出——在 [第 4 章](04-distributed-basics.md#_04-4-工程实操-跑起来与排障)的骨架上加三行：

```python
model = ...
for data in loader:                      # 每卡各自的数据分片
    loss = model(data).loss
    loss.backward()                      # 各算各的梯度
    for p in model.parameters():         # ★ 全量梯度取平均
        dist.all_reduce(p.grad)          #   （world group 上）
        p.grad /= world_size
    optimizer.step()                     # 同步更新 → 副本保持一致
    optimizer.zero_grad()
```

数学上完全正确。但性能上有个硬伤：**反向全部结束才发起第一次通信**——通信 M 秒 + 反向 N 秒 串行相加，一步 = N+M。生产级 DDP 的全部工作就是消灭这个串行。

::: warning 别用错 DP
`torch.nn.DataParallel`（单进程多线程）受 GIL 与线程调度拖累，早已不推荐；本章的 `DistributedDataParallel`（DDP）= 多进程 + 通信库，与第 4 章"一进程一卡"对齐。
:::

### 4. 生产级 DDP 多做的三件事

`DistributedDataParallel(model, device_ids=[local_rank])` 包一层，它做了：

1. **起点对齐**：构造时把 rank 0 的参数 **Broadcast** 给全员——保证 N 个副本从同一起点出发（不然第 5.1 节的"同步"从第一步就不成立）；
2. **梯度就绪即通信**：给每个参数注册反向 hook——**某个参数的梯度一算完，立刻发起它的通信**，不等反向结束。通信与剩余层的反向并行，这是重叠的入口；
3. **Bucket 组织**：就绪即发的粒度不是"单参数"而是"桶"——按反向完成顺序攒桶、桶满即发（05-3 展开）。

从本章往下钻一层：DDP 发起的每次 AllReduce 最终走进 [HCCL 的 AllReduce 调用链](../ascend/hccl-source/06-allreduce-call-chain.md)（HCCL 源码 6）——框架侧"什么时候发"，通信库侧"怎么跑"，正好接成一条线。

```text
DDP（本章）：何时发 → 逐桶、就绪即发
HCCL（HCCL 源码 6）：怎么跑 → 算法选择 → 引擎执行 → 环形流水
```

## 05-3｜Bucket：分桶的艺术

::: info 本单元目标
围绕 **"桶大小的两端权衡"** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

"梯度就绪即通信"的粒度怎么选？两个极端都不可行——**Bucket 是折中的艺术**。

### 5. 两个极端的账

| 粒度 | 通信次数 | 每次大小 | 问题 |
| --- | --- | --- | --- |
| 按单参数 | 数千~数万次/步 | KB 级 | **α 主导**：每次启动成本 × 巨量次数（回扣 [《集合通信》03-3 分界线](../collective/03-tree-algorithms-selection.md#_03-3-选型地图-大小消息的分界线)） |
| 全量一次 | 1 次/步 | 全部梯度（GB 级） | **无法重叠**：必须等反向全部结束才能发（05-2 的朴素版硬伤） |
| **Bucket** ✅ | 梯度总量 ÷ 桶容量 | 默认 25 MB 级 | 大到 β 高效、小到反向早期就能发出第一桶 |

桶大小是一个**可调旋钮**（`bucket_cap_mb`，PyTorch 默认 25 MB）：桶大 → 次数少（带宽利用率高）但重叠窗口小；桶小 → 重叠充分但滑向小消息化。这与 [《集合通信》05-3 的 Chunk](../collective/05-topology-hierarchical-overlap.md#_05-3-chunk-与-channel-拆小、铺满) 是同一个"粒度权衡"在不同层的化身。

### 6. 桶的编排：按反向顺序装填

DDP 不是按模型定义顺序装桶，而是**按反向传播的完成顺序**：

1. **启动时重排**：DDP 分析（或首次反向时记录）各参数梯度的就绪顺序，按此顺序把参数映射进桶——**先算完的梯度，排进先出发的桶**；
2. **首桶刻意小**：让第一桶尽早凑满、尽早发出——流水线"尽快点燃"，后续桶均衡大小；
3. **桶满即发**：某桶内全部梯度就绪 → 立即对该桶发起一次 AllReduce（在 world group 上，Ring 算法，回扣 [《集合通信》02 章](../collective/02-ring-allreduce.md#_02-1-为什么是环-拓扑的自然性)）。

```text
反向完成顺序（从输出层往输入层）：
  out_proj → norm2 → mlp.w2 → mlp.w1 → norm1 → attn.wv → ... → embed

桶 0（小）：out_proj, norm2          ← 反向刚开始就凑满、就发出
桶 1：      mlp.w2, mlp.w1
桶 2：      norm1, attn.wv, ...
```

::: tip 一句话
**Bucket 把"何时通信"从"反向结束后"提前到"反向进行中"**，把"发什么"从"单参数"攒成"25 MB"——一次改动同时解决 α 主导与无法重叠两个极端问题。
:::

## 05-4｜通信计算重叠：把等待藏起来

::: info 本单元目标
围绕 **"DDP 重叠的三个条件为何天然成立"** 建立一条可以复述的因果链。读完后，尝试不看正文用一句话解释它。
:::

### 7. 双流时间线

[《单卡执行系统》07-4](../device/07-stream-event-async.md#_07-4-重叠的艺术-让设备闲不下来) 给过重叠的三个通用条件——DDP 是它们最经典的落地：

| 通用条件 | DDP 中的化身 |
| --- | --- |
| 资源独立 | 计算 stream 与通信 stream（HCCL 挂在通信流上，不占 AI Core） |
| 依赖允许 | 通信的是**已就绪的梯度**；依赖它的是 `optimizer.step()`——在反向全部结束之后 |
| 显式编排 | 桶满 → 记录 Event → 通信流等 Event 后发 AllReduce；step 前等最后一桶完成 |

```text
计算流：  [反向: 桶2的层][桶1的层][桶0的层]═══╗
                     └Event┘  └Event┘        ║ [optimizer.step]
通信流：  ·········[等桶0]═AllReduce═[等桶1]═AllReduce═╣
                                              ║
时间 ────────────────────────────────────────► max(反向, 通信) ≪ 反向+通信
```

**收益**：串行 N+M 变成约 `max(N, M)`。用 05-1 的算例（反向 10 s、通信 1 s）：一步从 11 s 回到约 10 s——通信被整段藏进反向。

### 8. 边界：藏不住的部分

重叠不是魔法，三条边界要记牢：

1. **通信 > 反向时藏不住**：小模型、大梯度、慢网络（如跨机 8 卡跑 7B 模型）——通信 M 超过反向 N，多出来的部分照样裸奔。出路：压通信量（梯度量化/压缩）或换算法（[HCCL 源码 5](../ascend/hccl-source/05-coll-algorithms.md)），更彻底的是让通信量本身降下来——**第 9 章 ZeRO/FSDP 的预告**；
2. **最后的同步点无法去掉**：`optimizer.step()` 必须等最后一桶——同步语义的下界，省不掉，只能靠桶的排布让它早到；
3. **梯度累积是免费省钱**：累积 K 步再通信 → 通信次数 ÷ K（数学上等价于 K 倍大 batch 的 DP，回扣 [《训练与推理系统》01-2](../systems/01-training-loop.md#_01-2-梯度累积与-adamw-状态)）。

::: tip 本章收束
DDP 一步的完整画像：**world group 上、逐桶发起的 Ring AllReduce，桶按反向顺序装填，通信流与计算流用 Event 编排重叠**。下一章进入 Tensor Parallel——通信从"步与步之间"走进"层与层内部"，从 world group 走进机内小组。
:::

## 本章总结

> 返回：[第 5 章首页](#)

### 把本章串成一条主线

请先合上各单元正文，沿"语义 → 实现 → 分桶 → 重叠"复述整章；遇到断点时，再回到对应单元查阅。

```text
语义（05-1）：完整模型 × 数据切片 → 梯度必然不同
   不 AllReduce = N 个各自漂移的模型（数学错误，不是慢）
   AllReduce(avg) ⇔ 大 batch SGD · 每卡流量 2S(N-1)/N 与 batch 无关
        ↓
实现（05-2）：朴素版 = 反向后全量 all_reduce（串行 N+M）
   DDP 三件事：Broadcast 起点对齐 / 梯度就绪即通信 / Bucket 组织
        ↓
分桶（05-3）：单参数（α 主导）与全量一次（无法重叠）的折中
   按反向顺序装桶 · 首桶小先点燃 · 桶满即发（25 MB 旋钮）
        ↓
重叠（05-4）：双流 + Event = max(N, M)
   藏不住的：通信>反向 / step 前的最后同步 / （可免的）梯度累积
```

### 9. 动手练习

#### 练习 1：算一步的账

7.5B 模型、BF16、batch 64×2048 token/卡、单机 8 卡（机内 200 GB/s）、反向约 10 s。计算：梯度总量、每卡通信流量、通信时间、重叠后一步的近似时长。

#### 练习 2：桶策略设计

某模型反向完成顺序为 `A→B→C→D→E→F`，梯度大小均为 10 MB。若桶容量 25 MB，给出一种装桶方案使第一桶尽早发出；若把首桶缩为 10 MB，对流水线点燃时间有什么影响？

#### 练习 3：瓶颈判断

Profiling 显示：反向 4 s、通信流忙 9 s 且从第 1 s 起就在飞、一步总长 10 s。判断瓶颈在哪，给出至少两条不改模型的优化方向。

### 10. 自测题

1. DP 切的是什么？每张卡上有什么？
2. 不做梯度 AllReduce 会发生什么？为什么说这是"数学错误"而非"性能问题"？
3. 写出 DP 等价于大 batch SGD 的公式。
4. DDP 每步的通信对象与每卡流量公式是什么？为什么与 batch 大小无关？
5. 朴素 DDP 的性能硬伤是什么？
6. 生产级 DDP 的三件事是什么？
7. 按单参数通信和全量一次通信各有什么问题？Bucket 如何折中？
8. DDP 按什么顺序装桶？为什么首桶要小？
9. DDP 重叠的三个通用条件分别对应什么？
10. 哪些通信是重叠藏不住的？各有什么出路？

::: details 自测答案

1. 切数据（global batch 均分）；每卡一份完整模型副本 + 自己的数据分片。
2. N 张卡出现 N 个不同模型且差异逐步放大；因为缺失的是算法本身的取平均步骤，正确性缺失而非效率缺失。
3. $\bar g=\frac1N\sum g^{(i)}$，$\theta\leftarrow\theta-\text{lr}\cdot\bar g$——等价于用整个 global batch 算一次梯度再更新。
4. 全量梯度 $S=2\times$参数量（BF16 字节）；每卡 $2S\frac{N-1}{N}$；通信量只由模型决定，batch 增大只增加反向时长、摊薄通信占比。
5. 通信必须等反向全部结束才发起，一步 = 反向 N + 通信 M 串行相加。
6. 构造时 Broadcast rank 0 参数对齐起点；梯度就绪即触发通信（backward hook）；Bucket 组织通信粒度。
7. 单参数：次数巨量、α 主导；全量一次：无法与反向重叠。Bucket 攒成 25 MB 级：大到带宽高效、小到反向早期即可发出。
8. 按反向传播完成顺序（先算完先进桶）；首桶小使其尽早凑满发出、尽快点燃流水线，后续桶均衡。
9. 资源独立 = 计算/通信双流；依赖允许 = 通信已就绪梯度、消费者是 step；显式编排 = Event 同步 + step 前等最后一桶。
10. 通信时长超过反向的部分（出路：压缩/换算法/减量——ZeRO）；step 前的最后同步（语义下界，靠排布提前）；梯度累积可整段免掉。

:::

### 11. 本课小结

- DP 的通信是算法的一部分：AllReduce(avg) ⇔ 大 batch SGD；
- 每卡流量 $2S\frac{N-1}{N}$ 与 batch 无关——DP 的扩展性来自计算摊薄通信；
- DDP 三件事：起点对齐、就绪即发、分桶组织；
- Bucket 解决"α 主导 vs 无法重叠"的两难，按反向顺序装填、首桶小先点燃；
- 双流重叠把 N+M 压到 max(N,M)；藏不住的部分各有各的出路。

### 参考资料

- [PyTorch DDP 文档](https://docs.pytorch.org/docs/stable/generated/torch.nn.parallel.DistributedDataParallel.html)
- [《模型全景》02-4：梯度与通信](../model/02-tensor-autograd.md#_02-4-梯度-shape-与集合通信)
- [《训练与推理系统》01-4：显存与通信](../systems/01-training-loop.md#_01-4-训练显存与梯度通信)
- [《单卡执行系统》07-4：重叠的艺术](../device/07-stream-event-async.md#_07-4-重叠的艺术-让设备闲不下来)
- [《集合通信》02 章：Ring AllReduce](../collective/02-ring-allreduce.md#_02-1-为什么是环-拓扑的自然性)
- [HCCL 源码 6：AllReduce 调用链](../ascend/hccl-source/06-allreduce-call-chain.md)

下一章进入 **Tensor Parallel**：模型本体登场——Column/Row Parallel 怎样切矩阵、为什么一层之内就产生 AllGather、AllReduce 或 ReduceScatter，以及为什么 TP 组必须住在一台机器里。

---

<!-- chapter-navigation -->
